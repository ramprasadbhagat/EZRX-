import 'package:flutter/material.dart';

class ViewByOrdersPage extends StatelessWidget {
  const ViewByOrdersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
